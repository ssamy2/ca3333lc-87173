# API Configuration  
# Direct API URL - domain should be whitelisted for CORS
VITE_API_BASE_URL=http://207.180.203.9:5000

# Optional: Force proxy usage (only if needed)
# VITE_FORCE_PROXY=true

# Optional: Custom CORS proxy (only if FORCE_PROXY is true)
# VITE_CORS_PROXY=https://your-proxy.workers.dev